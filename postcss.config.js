export default {
  plugins: {
    tailwindcss: {},   // Configura o Tailwind CSS como 
    //plugin, permitindo o uso das classes utilitárias do Tailwind.
    autoprefixer: {},  // Configura o Autoprefixer, que adiciona automaticamente 
    //prefixos de CSS para compatibilidade com diferentes navegadores.
  },
}
